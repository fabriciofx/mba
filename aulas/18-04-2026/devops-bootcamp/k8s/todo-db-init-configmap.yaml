apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-db-init
  namespace: todo
data:
  init.sql: |
    create table if not exists todos (
      id serial primary key,
      title text not null,
      done boolean not null default false
    );
